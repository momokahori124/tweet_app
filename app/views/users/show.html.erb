<div class="main user-show">
  <div class="container">
    <div class="user">
      <h2><%= @user.name %></h2>
      <p><%= @user.email %></p>

        <!--- リンク --->
        <% if @current_user.id == @user.id %>
            <%=link_to("edit", "/users/#{@user.id}/edit")%>
        <% end %>
    </div>
  </div>
</div>